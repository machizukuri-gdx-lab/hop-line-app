rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // TODO: 開発用の全許可ルール。本番前に適切なルールに変更すること
      allow read, write: if true;
    }
  }
}
